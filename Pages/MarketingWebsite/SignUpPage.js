const SignUpPage = function(){

    const firstName = element(by.name(''));
    const lasTtName = element(by.name(''));
    const email = element(by.name(''));
    const password = element(by.name(''));
    const signUpButton = element(by.linkText(''));
}